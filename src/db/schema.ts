import { users } from "../modules/user_access_management/persistence/user/models/userModel";
import { animes } from "../modules/anime/persistence/anime/model/animeModel";

export const schema = {
  users,
  animes,
};

export { users, animes };
